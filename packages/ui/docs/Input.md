---
category: Forms
---

# Input

A single-line text field. Paper: 4px radius, hairline border, body type at full size rather than a shrunken UI size.

Passing `error` puts the field in its error state *and* renders the message — the two are deliberately not separable, because colour alone must never carry the state.
