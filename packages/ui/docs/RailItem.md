---
category: Layout
---

# RailItem

One destination in the `Rail`. Exactly one should be `active` at a time.

Active state is carried by paint and a hard edge, not by font weight — that difference is legible across a room, which a 100-unit weight step is not.
